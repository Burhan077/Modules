#!/bin/bash
# filectl - easily reset file ownership to user (UID 1000)

if [[ $# -eq 0 ]]; then
  echo "Usage: filectl <file_or_directory> [more_paths...]"
  exit 1
fi

sudo chmod +x "$@"
	         	
